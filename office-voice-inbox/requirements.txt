# Core capture + transcription
sounddevice>=0.4.6
webrtcvad>=2.0.10
requests>=2.31

# Drive "docs" backend (skip if using DRIVE_BACKEND=rclone)
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1

# Menu-bar toggle (macOS only; optional)
rumps>=0.4
